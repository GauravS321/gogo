<div class="content-i">
    <div class="content-box">
        {{#if success_msg}}
        <div class="alert alert-success" role="alert">{{success_msg}}</div>
        {{/if}}
        {{#if error_msg}}
        <div class="alert alert-warning" role="alert">{{error_msg}}</div>
        {{/if}}
        <div class="element-box" id="revokeWritePermissions">
            <h5 class="form-header">Revoke write permissions</h5>
            To revoke an entity's write permission to a data channel, fill in the form below and click on "Revoke".
            <br>
            <hr>
            <form id="formValidate" action="/components/data-channels/revoke" method="POST">
                <legend>
                    <span>Trade channel writer *</span>
                </legend>

                <div class="form-group">
                    <input class="form-control" type="text" autocomplete="off" data-error="This field is compulsory"
                        id="trade_channel_writer" name="trade_channel_writer" required>

                    <div class="help-block form-text text-muted">
                        The primechain address of the entity whose write permission is to be revoked.
                    </div>

                    <div class=" help-block form-text with-errors form-control-feedback"></div>
                </div>

                <div class="form-desc"></div>

                <legend>
                    <span>2. Data channel name *</span>
                </legend>

                <div class="form-group">
                    <input class="form-control" type="text" autocomplete="off" data-error="This field is compulsory"
                        id="trade_channel_name" name="trade_channel_name" required>

                    <div class="help-block form-text text-muted">
                        The name of the relevant data channel.
                    </div>

                    <div class="help-block form-text with-errors form-control-feedback"></div>
                </div>

                <div class="form-buttons-w">
                    <button class="btn btn-primary"> Revoke</button>
                </div>

            </form>
        </div>
    </div>
</div>