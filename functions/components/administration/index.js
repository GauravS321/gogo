const blockchainParameters = require('./blockchain_parameters');
const runtimeParameters = require('./runtime_parameters');
const blockchainInformation = require('./blockchain_information');

module.exports = {
    blockchainParameters,
    runtimeParameters,
    blockchainInformation
}